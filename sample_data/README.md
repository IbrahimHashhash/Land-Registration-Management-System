# Sample Data

- `mongodb/`   JSON files for each collection (land_applications, parcels,
               applicants, staff_members, survey_tasks, certificates,
               performance_logs) and a seed script.
- `postman/`   Postman collection + environment for testing the API.
